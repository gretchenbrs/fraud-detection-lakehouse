resources:
  dashboards:
    fraud_risk_analytics:
      display_name: "Financial Transaction Risk Analytics"
      file_path: ../dashboards/fraud_risk_analytics.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog_name}
      dataset_schema: ${var.schema_name}
